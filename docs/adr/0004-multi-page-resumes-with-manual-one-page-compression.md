# Multi-Page Resumes with Manual One-Page Compression

A Resume Document may span up to two pages — overflow is not blocked — and a per-document compression control shrinks the whole resume in 10% steps, persisted, so any document can be pulled down to a single page when needed. We chose manual incremental steps over auto-fit so the exported PDF always mirrors what the user saw and page geometry stays predictable.
